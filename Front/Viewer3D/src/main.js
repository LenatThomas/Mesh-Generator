import { GalleryControl } from './galleryControl'
document.addEventListener('DOMContentLoaded', () => {
  console.log('DOM loaded, initializing MeshControl');
  const galleryController = new GalleryControl();
});